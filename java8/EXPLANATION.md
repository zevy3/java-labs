# ПОЛНОЕ ОБЪЯСНЕНИЕ ПРОЕКТА ДЛЯ НОВИЧКОВ

## 📚 ОГЛАВЛЕНИЕ
1. Что такое аннотации
2. Что такое Stream API
3. Что такое многопоточность
4. Разбор каждого файла построчно
5. Как всё работает вместе

---

## 1️⃣ ЧТО ТАКОЕ АННОТАЦИИ

### Простыми словами:
Аннотация — это как "наклейка" или "ярлык", который мы клеим на код.
Представь, что у тебя есть много коробок (методов), и ты клеишь на некоторые
наклейку "ВАЖНОЕ". Потом программа может найти все коробки с этой наклейкой.

### Пример из жизни:
```
📦 Коробка 1 [ХРУПКОЕ] ← это аннотация
📦 Коробка 2
📦 Коробка 3 [ХРУПКОЕ] ← это аннотация
```
Курьер знает, что коробки с наклейкой [ХРУПКОЕ] нужно нести осторожно.

### В Java:
```java
@DataProcessor  ← это аннотация
public void обработатьДанные() {
    // код
}
```

---

## 2️⃣ ЧТО ТАКОЕ STREAM API

### Простыми словами:
Stream API — это как конвейер на заводе. Данные идут по "ленте", 
и на каждом этапе с ними что-то делают.

### Пример из жизни:
```
Яблоки → [Мойка] → [Сортировка] → [Упаковка] → Готовый продукт
```

### В Java:
```java
данные.stream()          // кладём на конвейер
      .filter(...)       // фильтруем (отбираем нужные)
      .map(...)          // преобразуем (меняем)
      .collect(...)      // собираем результат
```

---

## 3️⃣ ЧТО ТАКОЕ МНОГОПОТОЧНОСТЬ

### Простыми словами:
Представь, что у тебя одна задача: помыть 100 тарелок.
- **Однопоточность**: ты моешь один, тарелка за тарелкой. Долго!
- **Многопоточность**: ты позвал 3 друзей, и вы моете по 25 тарелок каждый. Быстрее!

### В программировании:
- **Поток (Thread)** — это как один работник
- **ExecutorService** — это как бригадир, который раздаёт задачи работникам
- **Future** — это как "обещание" получить результат, когда работник закончит

---

## 4️⃣ РАЗБОР КАЖДОГО ФАЙЛА ПОСТРОЧНО

---

### 📄 ФАЙЛ 1: DataProcessor.java (Аннотация)

```java
package com.example.dataprocessor;
```
**Что это?** Package — это как папка для организации файлов.
**Зачем?** Чтобы не было путаницы, если у нас 1000 классов.

```java
import java.lang.annotation.ElementType;
import java.lang.annotation.Retention;
import java.lang.annotation.RetentionPolicy;
import java.lang.annotation.Target;
```
**Что это?** Import — мы "импортируем" (подключаем) готовые инструменты из Java.
**Зачем?** Эти инструменты нужны для создания аннотаций.

```java
@Retention(RetentionPolicy.RUNTIME)
```
**Что это?** Мета-аннотация (аннотация для аннотации).
**Зачем?** Говорит: "Эта аннотация должна работать во время выполнения программы".
**Аналогия:** Наклейка не сотрётся, когда посылка будет в пути.

```java
@Target(ElementType.METHOD)
```
**Что это?** Указываем, куда можно клеить эту аннотацию.
**Зачем?** METHOD = только на методы (функции), не на классы или переменные.

```java
public @interface DataProcessor {
```
**Что это?** Объявляем нашу аннотацию с именем DataProcessor.
**@ interface** — специальный синтаксис для создания аннотаций.

```java
    String name() default "";
```
**Что это?** Параметр аннотации — можно указать имя обработчика.
**default ""** = если не указать, будет пустая строка.
**Как использовать:** `@DataProcessor(name = "Мой обработчик")`

```java
    int priority() default 0;
```
**Что это?** Приоритет обработки (число).
**Зачем?** Можно сортировать обработчики по важности.

---

### 📄 ФАЙЛ 2: DataManager.java (Главный управляющий класс)

```java
package com.example.dataprocessor;
```
Та же папка, что и раньше.

```java
import java.io.*;
```
**Что это?** Подключаем инструменты для работы с файлами (чтение/запись).
**io** = Input/Output (Ввод/Вывод).

```java
import java.lang.reflect.Method;
```
**Что это?** Reflection — возможность "заглянуть внутрь" класса во время работы.
**Зачем?** Чтобы найти методы с аннотацией @DataProcessor.

```java
import java.nio.file.Files;
import java.nio.file.Paths;
```
**Что это?** Современные инструменты для работы с файлами (nio = New IO).

```java
import java.util.*;
```
**Что это?** Коллекции: List, ArrayList, Map и т.д.
**Зачем?** Для хранения данных в списках.

```java
import java.util.concurrent.*;
```
**Что это?** Инструменты для многопоточности.
**concurrent** = одновременный, параллельный.

```java
import java.util.stream.Collectors;
import java.util.stream.Stream;
```
**Что это?** Stream API для обработки данных.

```java
public class DataManager {
```
**Что это?** Объявляем класс — "чертёж" для создания объектов.
**public** = доступен отовсюду.

```java
    private List<String> data;
```
**Что это?** Переменная для хранения данных (список строк).
**private** = доступна только внутри этого класса.
**List<String>** = список, который содержит строки (String).

```java
    private List<Object> processors;
```
**Что это?** Список объектов-обработчиков.
**Object** = любой тип объекта (универсальный).

```java
    private ExecutorService executorService;
```
**Что это?** "Бригадир", который управляет потоками.
**ExecutorService** = сервис исполнения задач.

```java
    public DataManager() {
```
**Что это?** Конструктор — специальный метод, который вызывается при создании объекта.
**Когда вызывается:** `new DataManager()` ← тут!

```java
        this.data = new ArrayList<>();
```
**Что это?** Создаём пустой список для данных.
**this** = "этот объект" (обращение к переменной класса).
**ArrayList** = реализация списка (динамический массив).

```java
        this.processors = new ArrayList<>();
        this.executorService = Executors.newFixedThreadPool(4);
```
**Что это?** Создаём список обработчиков и пул из 4 потоков.
**newFixedThreadPool(4)** = создать 4 "работника" для выполнения задач.

```java
    public void registerDataProcessor(Object processor) {
```
**Что это?** Метод для регистрации обработчика.
**void** = ничего не возвращает.
**Object processor** = принимает любой объект.

```java
        System.out.println("Регистрация обработчика: " + processor.getClass().getSimpleName());
```
**Что это?** Выводим в консоль сообщение.
**getClass()** = получить класс объекта.
**getSimpleName()** = получить короткое имя класса (без package).

```java
        processors.add(processor);
```
**Что это?** Добавляем обработчик в список.

```java
    public void loadData(String source) throws IOException {
```
**Что это?** Метод для загрузки данных из файла.
**String source** = путь к файлу.
**throws IOException** = может выбросить ошибку ввода-вывода.

```java
        System.out.println("Загрузка данных из: " + source);
        try (BufferedReader reader = new BufferedReader(new FileReader(source))) {
```
**Что это?** try-with-resources — автоматически закроет файл после использования.
**BufferedReader** = читатель с буфером (читает текст эффективно).
**FileReader** = читает из файла.

```java
            data = reader.lines().collect(Collectors.toList());
```
**Что это?** Stream API в действии!
- **reader.lines()** — создаёт поток (stream) из строк файла
- **.collect(Collectors.toList())** — собирает всё в список

```java
        System.out.println("Загружено строк: " + data.size());
```
**Что это?** Выводим количество загруженных строк.
**data.size()** = размер списка.

```java
    public void processData() throws Exception {
```
**Что это?** Метод для обработки данных (главная логика).

```java
        System.out.println("\n=== Начало обработки данных ===");
```
**\n** = перенос строки.

```java
        List<Future<List<String>>> futures = new ArrayList<>();
```
**Что это?** Список "обещаний" (Future).
**Future<List<String>>** = обещание получить список строк в будущем.
**Зачем?** Потоки работают параллельно, результат придёт позже.

```java
        for (Object processor : processors) {
```
**Что это?** Цикл по всем зарегистрированным обработчикам.
**for (тип элемент : коллекция)** — современный цикл foreach.

```java
            Method[] methods = processor.getClass().getDeclaredMethods();
```
**Что это?** Получаем все методы класса обработчика.
**Reflection в действии!**
**Method[]** = массив методов.

```java
            for (Method method : methods) {
```
**Что это?** Цикл по всем методам.

```java
                if (method.isAnnotationPresent(DataProcessor.class)) {
```
**Что это?** Проверяем: есть ли на методе аннотация @DataProcessor?
**isAnnotationPresent** = присутствует ли аннотация.

```java
                    DataProcessor annotation = method.getAnnotation(DataProcessor.class);
```
**Что это?** Получаем саму аннотацию, чтобы прочитать её параметры.

```java
                    Future<List<String>> future = executorService.submit(() -> {
```
**Что это?** Отправляем задачу на выполнение в пул потоков.
**submit** = отправить.
**() -> {}** = лямбда-выражение (короткая анонимная функция).

```java
                        System.out.println("Поток " + Thread.currentThread().getName() + 
                                         " обрабатывает: " + annotation.name());
```
**Что это?** Выводим, какой поток выполняет задачу.
**Thread.currentThread()** = текущий поток.
**getName()** = получить имя потока.

```java
                        try {
                            @SuppressWarnings("unchecked")
                            List<String> result = (List<String>) method.invoke(processor, data);
```
**Что это?** Вызываем метод обработчика.
**method.invoke(объект, параметры)** = вызвать метод через Reflection.
**(List<String>)** = приведение типа (casting).
**@SuppressWarnings** = подавить предупреждение компилятора.

```java
                            System.out.println("Завершена обработка: " + annotation.name() + 
                                             ", результат: " + result.size() + " элементов");
                            return result;
```
**Что это?** Выводим результат и возвращаем список.

```java
                        } catch (Exception e) {
                            System.err.println("Ошибка в методе " + annotation.name() + ": " + e.getMessage());
                            return new ArrayList<>();
                        }
```
**Что это?** Обработка ошибок.
**catch** = поймать исключение.
**System.err** = вывод ошибок (красным).

```java
                    });
                    
                    futures.add(future);
```
**Что это?** Добавляем "обещание" в список.

```java
        List<String> allResults = new ArrayList<>();
        for (Future<List<String>> future : futures) {
```
**Что это?** Собираем все результаты из "обещаний".

```java
            try {
                allResults.addAll(future.get());
```
**Что это?** Получаем результат из Future.
**future.get()** = ЖДЁТ, пока задача не завершится, и возвращает результат.
**addAll** = добавить все элементы.

```java
            } catch (Exception e) {
                System.err.println("Ошибка получения результата: " + e.getMessage());
            }
```
Обработка ошибок.

```java
        data = allResults;
```
**Что это?** Заменяем исходные данные обработанными.

```java
        System.out.println("=== Обработка завершена, всего результатов: " + data.size() + " ===\n");
```
Выводим итоговое сообщение.

```java
    public void saveData(String destination) throws IOException {
```
**Что это?** Метод для сохранения данных в файл.

```java
        System.out.println("Сохранение данных в: " + destination);
        try (BufferedWriter writer = new BufferedWriter(new FileWriter(destination))) {
```
**BufferedWriter** = писатель с буфером (пишет эффективно).

```java
            for (String line : data) {
                writer.write(line);
                writer.newLine();
            }
```
**Что это?** Записываем каждую строку в файл.
**newLine()** = перевод строки.

```java
        System.out.println("Сохранено строк: " + data.size());
```
Сообщение о завершении.

```java
    public void shutdown() {
```
**Что это?** Метод для корректного завершения работы потоков.

```java
        executorService.shutdown();
```
**Что это?** Говорим ExecutorService: "Больше не принимай новые задачи".

```java
        try {
            if (!executorService.awaitTermination(60, TimeUnit.SECONDS)) {
```
**Что это?** Ждём 60 секунд, пока все задачи завершатся.
**awaitTermination** = ждать завершения.

```java
                executorService.shutdownNow();
```
**Что это?** Если не дождались — принудительно останавливаем.

```java
        } catch (InterruptedException e) {
            executorService.shutdownNow();
        }
```
Обработка прерывания.

```java
    public List<String> getData() {
        return new ArrayList<>(data);
    }
```
**Что это?** Метод для получения данных.
**new ArrayList<>(data)** = возвращаем копию, чтобы внешний код не мог изменить оригинал.

---

### 📄 ФАЙЛ 3: FilterProcessor.java (Обработчик фильтрации)

```java
public class FilterProcessor {
```
Класс с методами для фильтрации данных.

```java
    @DataProcessor(name = "Фильтр длинных строк", priority = 1)
```
**Что это?** Вешаем аннотацию на метод.
**name** = имя обработчика.
**priority** = приоритет.

```java
    public List<String> filterLongStrings(List<String> data) {
```
**Что это?** Метод принимает список строк, возвращает список строк.

```java
        return data.stream()
```
**Что это?** Создаём поток из списка.

```java
                .filter(s -> s.length() > 5)
```
**Что это?** Фильтруем: оставляем только строки длиной больше 5.
**s -> ...** = лямбда: для каждой строки s проверяем условие.

```java
                .collect(Collectors.toList());
```
**Что это?** Собираем отфильтрованные элементы обратно в список.

```java
    @DataProcessor(name = "Фильтр строк с цифрами", priority = 2)
    public List<String> filterStringsWithNumbers(List<String> data) {
        return data.stream()
                .filter(s -> s.matches(".*\\d.*"))
                .collect(Collectors.toList());
    }
```
**Что это?** Фильтр: оставляем строки, содержащие цифры.
**matches(".*\\d.*")** = регулярное выражение:
- **.*** = любые символы
- **\\d** = цифра
- **.*** = любые символы
Итого: "любые символы, потом цифра, потом любые символы".

```java
    @DataProcessor(name = "Фильтр непустых строк", priority = 3)
    public List<String> filterNonEmpty(List<String> data) {
        return data.stream()
                .filter(s -> !s.trim().isEmpty())
                .collect(Collectors.toList());
    }
```
**Что это?** Фильтр непустых строк.
**trim()** = удалить пробелы с начала и конца.
**isEmpty()** = пустая ли строка?
**!** = отрицание (НЕ пустая).

---

### 📄 ФАЙЛ 4: TransformProcessor.java (Обработчик преобразования)

```java
    @DataProcessor(name = "Преобразование в верхний регистр", priority = 1)
    public List<String> toUpperCase(List<String> data) {
        return data.stream()
                .map(String::toUpperCase)
                .collect(Collectors.toList());
    }
```
**Что это?** Преобразование всех строк в ВЕРХНИЙ РЕГИСТР.
**map** = преобразовать каждый элемент.
**String::toUpperCase** = ссылка на метод (method reference).
Эквивалентно: **s -> s.toUpperCase()**

```java
    @DataProcessor(name = "Добавление префикса", priority = 2)
    public List<String> addPrefix(List<String> data) {
        return data.stream()
                .map(s -> "[PROCESSED] " + s)
                .collect(Collectors.toList());
    }
```
**Что это?** Добавляем префикс "[PROCESSED] " к каждой строке.

```java
    @DataProcessor(name = "Обрезка пробелов", priority = 3)
    public List<String> trimSpaces(List<String> data) {
        return data.stream()
                .map(String::trim)
                .collect(Collectors.toList());
    }
```
**Что это?** Убираем пробелы с начала и конца каждой строки.

---

### 📄 ФАЙЛ 5: AggregationProcessor.java (Обработчик агрегации)

```java
    @DataProcessor(name = "Группировка по длине", priority = 1)
    public List<String> groupByLength(List<String> data) {
        return data.stream()
                .collect(Collectors.groupingBy(String::length))
```
**Что это?** Группируем строки по длине.
**groupingBy** = сгруппировать по критерию.
**String::length** = по длине строки.
**Результат:** Map<Integer, List<String>> — ключ: длина, значение: список строк этой длины.

```java
                .entrySet()
                .stream()
```
**Что это?** Получаем набор пар ключ-значение и создаём из него поток.
**entrySet()** = набор "записей" (ключ + значение).

```java
                .map(entry -> "Длина " + entry.getKey() + ": " + entry.getValue().size() + " элементов")
```
**Что это?** Преобразуем каждую группу в строку.
**entry.getKey()** = длина строки.
**entry.getValue().size()** = количество строк этой длины.

```java
                .collect(Collectors.toList());
```
Собираем в список.

```java
    @DataProcessor(name = "Подсчет статистики", priority = 2)
    public List<String> calculateStatistics(List<String> data) {
        long count = data.size();
```
**Что это?** Считаем количество элементов.

```java
        double avgLength = data.stream()
                .mapToInt(String::length)
```
**Что это?** Преобразуем каждую строку в её длину (число).
**mapToInt** = преобразовать в int.

```java
                .average()
```
**Что это?** Вычисляем среднее значение.

```java
                .orElse(0.0);
```
**Что это?** Если нет элементов, вернуть 0.0.
**orElse** = или иначе.

```java
        int maxLength = data.stream()
                .mapToInt(String::length)
                .max()
                .orElse(0);
```
**Что это?** Находим максимальную длину.

```java
        return List.of(
            "Статистика:",
            "Всего элементов: " + count,
            "Средняя длина: " + String.format("%.2f", avgLength),
            "Максимальная длина: " + maxLength
        );
```
**Что это?** Возвращаем список строк со статистикой.
**List.of(...)** = создать неизменяемый список.
**String.format("%.2f", ...)** = форматирование числа (2 знака после запятой).

```java
    @DataProcessor(name = "Уникальные значения", priority = 3)
    public List<String> getUniqueValues(List<String> data) {
        return data.stream()
                .distinct()
```
**Что это?** Убираем дубликаты.
**distinct()** = различные, уникальные.

```java
                .sorted()
```
**Что это?** Сортируем по алфавиту.

```java
                .collect(Collectors.toList());
```
Собираем в список.

---

### 📄 ФАЙЛ 6: Main.java (Точка входа в программу)

```java
public class Main {
    public static void main(String[] args) {
```
**Что это?** Главный метод — с него начинается выполнение программы.
**static** = принадлежит классу, а не объекту.
**String[] args** = аргументы командной строки.

```java
        try {
```
**Что это?** Начинаем блок обработки ошибок.

```java
            DataManager dataManager = new DataManager();
```
**Что это?** Создаём объект DataManager.

```java
            dataManager.registerDataProcessor(new FilterProcessor());
            dataManager.registerDataProcessor(new TransformProcessor());
            dataManager.registerDataProcessor(new AggregationProcessor());
```
**Что это?** Регистрируем все обработчики.

```java
            dataManager.loadData("data/input.txt");
```
**Что это?** Загружаем данные из файла.

```java
            dataManager.processData();
```
**Что это?** Обрабатываем данные (многопоточно).

```java
            dataManager.saveData("data/output.txt");
```
**Что это?** Сохраняем результат в файл.

```java
            dataManager.shutdown();
```
**Что это?** Корректно завершаем работу потоков.

```java
            System.out.println("\n✓ Приложение успешно завершено!");
```
Сообщение об успехе.

```java
        } catch (Exception e) {
            System.err.println("Ошибка: " + e.getMessage());
            e.printStackTrace();
        }
```
**Что это?** Ловим все ошибки.
**printStackTrace()** = вывести полную информацию об ошибке.

---

## 5️⃣ КАК ВСЁ РАБОТАЕТ ВМЕСТЕ

### Шаг 1: Запуск программы
```
java Main → вызывается метод main()
```

### Шаг 2: Инициализация
```
DataManager создаётся:
- Пустой список данных
- Пустой список обработчиков
- Пул из 4 потоков готов к работе
```

### Шаг 3: Регистрация обработчиков
```
registerDataProcessor(new FilterProcessor())
  ↓
processors = [FilterProcessor, TransformProcessor, AggregationProcessor]
```

### Шаг 4: Загрузка данных
```
loadData("data/input.txt")
  ↓
Читаем файл построчно
  ↓
data = ["Hello World", "Java 8 Programming", ...]
```

### Шаг 5: Обработка данных (САМОЕ ИНТЕРЕСНОЕ!)
```
processData() вызывается
  ↓
Для каждого обработчика:
  ↓
  Для каждого метода с @DataProcessor:
    ↓
    Отправляем задачу в пул потоков
    ↓
    Поток 1: Выполняет "Фильтр длинных строк"
    Поток 2: Выполняет "Фильтр строк с цифрами"
    Поток 3: Выполняет "Преобразование в верхний регистр"
    Поток 4: Выполняет "Добавление префикса"
    ↓
    (Всё это ПАРАЛЛЕЛЬНО! Одновременно!)
    ↓
    Каждый поток возвращает Future<List<String>>
  ↓
Ждём, пока все потоки закончат (future.get())
  ↓
Собираем все результаты в один список
  ↓
data = [все обработанные строки]
```

### Шаг 6: Сохранение
```
saveData("data/output.txt")
  ↓
Записываем каждую строку из data в файл
```

### Шаг 7: Завершение
```
shutdown()
  ↓
Закрываем пул потоков
  ↓
Программа завершена!
```

---

## 🎯 КЛЮЧЕВЫЕ КОНЦЕПЦИИ

### 1. Reflection (Рефлексия)
**Что?** Возможность программы "осмотреть саму себя".
**Зачем?** Найти методы с аннотацией @DataProcessor.
**Код:**
```java
Method[] methods = processor.getClass().getDeclaredMethods();
if (method.isAnnotationPresent(DataProcessor.class)) { ... }
```

### 2. Stream API
**Что?** Функциональный подход к обработке коллекций.
**Операции:**
- **filter** — отбор элементов
- **map** — преобразование элементов
- **collect** — сбор результата
- **distinct** — уникальные элементы
- **sorted** — сортировка

### 3. Многопоточность
**Что?** Параллельное выполнение задач.
**Компоненты:**
- **ExecutorService** — управляет потоками
- **Future** — результат асинхронной задачи
- **Lambda (->)** — короткая анонимная функция

### 4. Аннотации
**Что?** Метаданные для кода.
**Зачем?** Пометить методы для автоматической обработки.

---

## 📊 ВИЗУАЛИЗАЦИЯ ПОТОКА ДАННЫХ

```
Input File (data/input.txt)
         ↓
    loadData()
         ↓
   List<String> data
         ↓
   processData()
         ↓
    ┌────────────────────────────┐
    │   ExecutorService          │
    │   (4 потока)               │
    ├────────────────────────────┤
    │ Thread-1: FilterProcessor  │
    │ Thread-2: TransformProc    │
    │ Thread-3: AggregationProc  │
    │ Thread-4: FilterProcessor  │
    └────────────────────────────┘
         ↓
   Все результаты собираются
         ↓
   List<String> processedData
         ↓
    saveData()
         ↓
Output File (data/output.txt)
```

---

## 🔑 ОСНОВНЫЕ ТЕРМИНЫ

| Термин | Простое объяснение |
|--------|-------------------|
| **Stream** | Конвейер для обработки данных |
| **Lambda** | Короткая функция: `x -> x * 2` |
| **Reflection** | "Зеркало" для кода — можем посмотреть на себя |
| **Thread** | Отдельный "работник" |
| **Future** | "Обещание" получить результат позже |
| **Annotation** | "Наклейка" на коде |
| **Filter** | Отбор нужных элементов |
| **Map** | Преобразование элементов |
| **Collect** | Сбор результата |

---

## ✅ ПРОВЕРКА ПОНИМАНИЯ

### Вопрос 1: Что делает этот код?
```java
data.stream().filter(s -> s.length() > 5).collect(Collectors.toList());
```
**Ответ:** Берёт список data, оставляет только строки длиннее 5 символов, собирает в новый список.

### Вопрос 2: Зачем нужен ExecutorService?
**Ответ:** Чтобы задачи выполнялись параллельно в нескольких потоках (быстрее).

### Вопрос 3: Что делает @DataProcessor?
**Ответ:** Помечает методы, чтобы DataManager мог их найти и выполнить.

### Вопрос 4: Зачем нужен Future?
**Ответ:** Потому что задача выполняется в другом потоке. Future — это "квитанция", 
по которой мы позже получим результат.

---

## 🎓 ИТОГО

Эта программа:
1. **Загружает** данные из файла
2. **Находит** все методы с @DataProcessor (через Reflection)
3. **Запускает** их параллельно в нескольких потоках
4. **Обрабатывает** данные с помощью Stream API
5. **Собирает** все результаты
6. **Сохраняет** в новый файл

**Главные фишки:**
- ✅ Аннотации — для гибкости
- ✅ Stream API — для удобной обработки
- ✅ Многопоточность — для скорости
- ✅ Reflection — для "магии" (автоматический поиск методов)

**Это отличный пример современной архитектуры Java 8+!**
