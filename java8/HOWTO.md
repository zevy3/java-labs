# 🚀 КАК ЗАПУСТИТЬ ПРОЕКТ

## Шаг 1: Компиляция

```bash
cd /home/zevarch/Yandex.Disk/МТУСИ/java-labs/java8
javac -d bin -sourcepath src/main/java src/main/java/com/example/dataprocessor/*.java
```

**Что происходит:**
- `javac` — компилятор Java
- `-d bin` — сохранить скомпилированные .class файлы в папку bin
- `-sourcepath` — где искать исходники
- `*.java` — компилировать все Java файлы

## Шаг 2: Запуск

```bash
java -cp bin com.example.dataprocessor.Main
```

**Что происходит:**
- `java` — запускаем программу
- `-cp bin` — classpath (где искать .class файлы)
- `com.example.dataprocessor.Main` — полное имя главного класса

---

## 📝 ЧТО МОЖНО ИЗМЕНИТЬ И ПОЭКСПЕРИМЕНТИРОВАТЬ

### 1. Измени входные данные
Открой файл `data/input.txt` и добавь свои строки:
```
Привет мир
Тестовая строка 123
Короткая
Очень длинная строка для проверки
```

### 2. Создай свой обработчик

Создай файл `MyCustomProcessor.java`:

```java
package com.example.dataprocessor;

import java.util.List;
import java.util.stream.Collectors;

public class MyCustomProcessor {
    
    @DataProcessor(name = "Мой обработчик", priority = 1)
    public List<String> reverseStrings(List<String> data) {
        return data.stream()
                .map(s -> new StringBuilder(s).reverse().toString())
                .collect(Collectors.toList());
    }
}
```

**Что делает:** Переворачивает строки задом наперёд!
- "Hello" → "olleH"
- "Java" → "avaJ"

Затем зарегистрируй его в Main.java:
```java
dataManager.registerDataProcessor(new MyCustomProcessor());
```

### 3. Измени количество потоков

В `DataManager.java` найди строку:
```java
this.executorService = Executors.newFixedThreadPool(4);
```

Измени 4 на другое число:
- `2` — меньше потоков (медленнее, но меньше нагрузка)
- `8` — больше потоков (быстрее, если много ядер процессора)

---

## 🧪 ЭКСПЕРИМЕНТЫ

### Эксперимент 1: Увидеть Stream API в действии

Добавь в любой метод обработчика промежуточный вывод:

```java
@DataProcessor(name = "Тест", priority = 1)
public List<String> test(List<String> data) {
    return data.stream()
            .peek(s -> System.out.println("До фильтра: " + s))
            .filter(s -> s.length() > 5)
            .peek(s -> System.out.println("После фильтра: " + s))
            .collect(Collectors.toList());
}
```

**peek** — это как "подсмотреть" данные на конвейере, не изменяя их.

### Эксперимент 2: Добавь счётчик обработанных элементов

```java
import java.util.concurrent.atomic.AtomicInteger;

public class CountingProcessor {
    private AtomicInteger counter = new AtomicInteger(0);
    
    @DataProcessor(name = "Счётчик", priority = 1)
    public List<String> countElements(List<String> data) {
        return data.stream()
                .peek(s -> {
                    int count = counter.incrementAndGet();
                    System.out.println("Обработано элементов: " + count);
                })
                .collect(Collectors.toList());
    }
}
```

**AtomicInteger** — специальный счётчик для многопоточности (безопасный).

### Эксперимент 3: Создай обработчик, который читает из базы данных (симуляция)

```java
public class DatabaseProcessor {
    
    @DataProcessor(name = "Симуляция БД", priority = 1)
    public List<String> simulateDatabase(List<String> data) {
        // Симулируем задержку БД
        return data.stream()
                .map(s -> {
                    try {
                        Thread.sleep(100); // 100 мс задержка
                    } catch (InterruptedException e) {
                        e.printStackTrace();
                    }
                    return "[FROM_DB] " + s;
                })
                .collect(Collectors.toList());
    }
}
```

Запусти с 1 потоком и с 4 потоками — увидишь разницу во времени!

---

## 🎯 ЗАДАЧИ ДЛЯ САМОПРОВЕРКИ

### Задача 1: Лёгкая
Создай обработчик, который считает, сколько строк содержат слово "Java".

<details>
<summary>Подсказка</summary>

```java
@DataProcessor(name = "Счёт Java", priority = 1)
public List<String> countJava(List<String> data) {
    long count = data.stream()
            .filter(s -> s.contains("Java"))
            .count();
    return List.of("Строк с 'Java': " + count);
}
```
</details>

### Задача 2: Средняя
Создай обработчик, который разбивает каждую строку на слова и считает общее количество слов.

<details>
<summary>Подсказка</summary>

```java
@DataProcessor(name = "Счёт слов", priority = 1)
public List<String> countWords(List<String> data) {
    long totalWords = data.stream()
            .flatMap(s -> Arrays.stream(s.split("\\s+")))
            .count();
    return List.of("Всего слов: " + totalWords);
}
```

**flatMap** — "расплющивает" вложенные потоки в один.
</details>

### Задача 3: Сложная
Создай обработчик, который находит топ-3 самых длинных слова во всех строках.

<details>
<summary>Подсказка</summary>

```java
@DataProcessor(name = "Топ-3 слова", priority = 1)
public List<String> top3Words(List<String> data) {
    return data.stream()
            .flatMap(s -> Arrays.stream(s.split("\\s+")))
            .sorted(Comparator.comparingInt(String::length).reversed())
            .distinct()
            .limit(3)
            .collect(Collectors.toList());
}
```

- **sorted** — сортируем
- **Comparator.comparingInt** — по длине
- **reversed()** — в обратном порядке (от большего к меньшему)
- **limit(3)** — берём только 3 элемента
</details>

---

## 🐛 ОТЛАДКА

### Если программа не компилируется:

1. **Проверь структуру папок:**
```
java8/
├── src/main/java/com/example/dataprocessor/
│   ├── DataProcessor.java
│   ├── DataManager.java
│   ├── FilterProcessor.java
│   ├── TransformProcessor.java
│   ├── AggregationProcessor.java
│   └── Main.java
└── data/
    └── input.txt
```

2. **Проверь package в начале каждого файла:**
```java
package com.example.dataprocessor;
```

### Если программа компилируется, но не запускается:

1. **Проверь classpath:**
```bash
java -cp bin com.example.dataprocessor.Main
```

2. **Проверь, что файл input.txt существует:**
```bash
ls data/input.txt
```

### Если видишь ошибку "NullPointerException":

Это значит, что какая-то переменная равна `null`. Проверь:
- Загрузились ли данные?
- Зарегистрированы ли обработчики?

### Если видишь ошибку "ConcurrentModificationException":

Это значит, что несколько потоков пытаются изменить один список одновременно.
Решение: используй `Collections.synchronizedList()` или `CopyOnWriteArrayList`.

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ МАТЕРИАЛЫ

### Stream API — основные методы:

| Метод | Что делает | Пример |
|-------|-----------|--------|
| `filter` | Отбор | `filter(s -> s.length() > 5)` |
| `map` | Преобразование | `map(String::toUpperCase)` |
| `flatMap` | "Расплющивание" | `flatMap(s -> Arrays.stream(s.split(" ")))` |
| `distinct` | Уникальные | `distinct()` |
| `sorted` | Сортировка | `sorted()` |
| `limit` | Ограничение | `limit(10)` |
| `skip` | Пропуск | `skip(5)` |
| `peek` | "Подсмотреть" | `peek(System.out::println)` |
| `count` | Подсчёт | `count()` |
| `collect` | Сбор | `collect(Collectors.toList())` |
| `forEach` | Для каждого | `forEach(System.out::println)` |

### Collectors — способы сбора:

| Метод | Что делает | Результат |
|-------|-----------|-----------|
| `toList()` | В список | `List<T>` |
| `toSet()` | В множество | `Set<T>` |
| `toMap()` | В карту | `Map<K, V>` |
| `joining()` | Склеить строки | `String` |
| `groupingBy()` | Группировка | `Map<K, List<V>>` |
| `counting()` | Подсчёт | `Long` |
| `summingInt()` | Сумма | `Integer` |
| `averagingInt()` | Среднее | `Double` |

---

## 🎓 ЧТО ДАЛЬШЕ?

После освоения этого проекта, можешь попробовать:

1. **Добавить обработку JSON**
   - Используй библиотеку Gson или Jackson
   - Загружай данные из JSON файла

2. **Подключить реальную базу данных**
   - Используй H2 (встроенная БД) или PostgreSQL
   - Загружай и сохраняй данные в БД

3. **Добавить веб-интерфейс**
   - Используй Spring Boot
   - Создай REST API для обработки данных

4. **Создать конфигурацию через файл**
   - Используй properties или YAML
   - Настраивай количество потоков, пути к файлам

5. **Добавить логирование**
   - Используй SLF4J + Logback
   - Записывай логи в файл

---

## ⚡ ПОЛЕЗНЫЕ КОМАНДЫ

### Компиляция и запуск одной командой:
```bash
javac -d bin -sourcepath src/main/java src/main/java/com/example/dataprocessor/*.java && java -cp bin com.example.dataprocessor.Main
```

### Очистка скомпилированных файлов:
```bash
rm -rf bin/*
```

### Просмотр структуры проекта:
```bash
tree
# или
find . -type f -name "*.java"
```

### Подсчёт строк кода:
```bash
find . -name "*.java" | xargs wc -l
```

---

## 💡 СОВЕТЫ

1. **Начни с простого** — сначала разберись с одним обработчиком, потом добавляй остальные.

2. **Экспериментируй** — меняй параметры, добавляй `System.out.println()` везде, где непонятно.

3. **Используй дебаггер** — в IntelliJ IDEA или Eclipse можно ставить точки останова и смотреть, что происходит.

4. **Читай ошибки** — они обычно точно говорят, что не так.

5. **Гугли** — если что-то непонятно, всегда можно найти объяснение.

---

## 🎉 ПОЗДРАВЛЯЮ!

Ты только что изучил:
- ✅ Аннотации
- ✅ Reflection
- ✅ Stream API
- ✅ Многопоточность
- ✅ ExecutorService и Future
- ✅ Лямбда-выражения
- ✅ Method references

Это продвинутые темы Java 8+. Отличная работа! 🚀
