# 🎓 Лабораторная работа: Обработка данных с использованием Stream API и многопоточности

## 📋 Описание проекта

Приложение для многопоточной обработки данных с использованием:
- **Аннотаций** (@DataProcessor)
- **Reflection** (динамический поиск методов)
- **Stream API** (функциональная обработка данных)
- **Многопоточности** (java.util.concurrent)

---

## 🚀 Быстрый старт

### 1. Компиляция
```bash
javac -d bin -sourcepath src/main/java src/main/java/com/example/dataprocessor/*.java
```

### 2. Запуск
```bash
java -cp bin com.example.dataprocessor.Main
```

### 3. Результат
Программа считывает `data/input.txt`, обрабатывает данные и сохраняет результат в `data/output.txt`.

---

## 📁 Структура проекта

```
java8/
├── src/main/java/com/example/dataprocessor/
│   ├── DataProcessor.java          # Аннотация для пометки методов
│   ├── DataManager.java             # Главный управляющий класс
│   ├── FilterProcessor.java         # Обработчик фильтрации
│   ├── TransformProcessor.java      # Обработчик преобразования
│   ├── AggregationProcessor.java    # Обработчик агрегации
│   └── Main.java                    # Точка входа
├── data/
│   ├── input.txt                    # Исходные данные
│   └── output.txt                   # Результат обработки
├── bin/                             # Скомпилированные .class файлы
├── EXPLANATION.md                   # 📖 ПОДРОБНОЕ объяснение КАЖДОЙ строки
├── VISUAL.md                        # 🎨 Визуальные диаграммы и схемы
├── HOWTO.md                         # 🛠️ Как запустить, изменить, экспериментировать
├── FAQ.md                           # ❓ Часто задаваемые вопросы
└── README.md                        # Этот файл
```

---

## 📚 Документация

### Для новичков (читать по порядку):

1. **README.md** (этот файл) — общий обзор проекта
2. **EXPLANATION.md** — построчное объяснение ВСЕГО кода с азов
3. **VISUAL.md** — диаграммы и визуальные схемы
4. **HOWTO.md** — как запустить и экспериментировать
5. **FAQ.md** — ответы на частые вопросы

### Что в каких файлах:

| Файл | Что внутри |
|------|-----------|
| **EXPLANATION.md** | 🔍 Детальное объяснение каждой строки кода |
| **VISUAL.md** | 🎨 Диаграммы архитектуры, схемы потоков данных |
| **HOWTO.md** | 🛠️ Команды запуска, эксперименты, задачи |
| **FAQ.md** | ❓ Ответы на вопросы "Почему?", "Зачем?", "Как?" |

---

## 🎯 Выполненные требования задания

### ✅ 1. Аннотация @DataProcessor
```java
@DataProcessor(name = "Имя обработчика", priority = 1)
public List<String> methodName(List<String> data) {
    // обработка
}
```

### ✅ 2. Класс DataManager
- `registerDataProcessor(Object)` — регистрация обработчиков
- `loadData(String)` — загрузка из файла
- `processData()` — многопоточная обработка
- `saveData(String)` — сохранение в файл

### ✅ 3. Обработчики данных
- **FilterProcessor** — фильтрация (длинные строки, с цифрами, непустые)
- **TransformProcessor** — преобразование (верхний регистр, префикс, обрезка)
- **AggregationProcessor** — агрегация (группировка, статистика, уникальные)

### ✅ 4. Многопоточность
Использован `ExecutorService` с пулом из 4 потоков для параллельной обработки.

### ✅ 5. Тестирование
Программа загружает 20 строк, применяет 9 обработчиков и выводит 143 результата.

---

## 🔑 Ключевые концепции

### 1. Аннотации
```java
@DataProcessor  // "Наклейка" на методе
```
Позволяет автоматически находить и вызывать методы обработки.

### 2. Reflection
```java
Method[] methods = processor.getClass().getDeclaredMethods();
if (method.isAnnotationPresent(DataProcessor.class)) {
    method.invoke(processor, data);
}
```
Программа "смотрит на саму себя" и находит нужные методы.

### 3. Stream API
```java
data.stream()
    .filter(s -> s.length() > 5)    // Фильтрация
    .map(String::toUpperCase)       // Преобразование
    .collect(Collectors.toList());  // Сбор результата
```
Функциональная обработка данных (как конвейер на заводе).

### 4. Многопоточность
```java
ExecutorService executor = Executors.newFixedThreadPool(4);
Future<Result> future = executor.submit(() -> {
    // Задача выполняется в отдельном потоке
});
Result result = future.get(); // Ждём результата
```
Параллельное выполнение задач для ускорения работы.

---

## 🧪 Примеры работы

### Входные данные (data/input.txt):
```
Hello World
Java 8 Programming
Stream API
Data Processing 123
Short
```

### Выходные данные (data/output.txt):
```
Hello World
Java 8 Programming
Data Processing 123
HELLO WORLD
JAVA 8 PROGRAMMING
[PROCESSED] Hello World
[PROCESSED] Java 8 Programming
Длина 11: 2 элементов
Длина 18: 1 элементов
Статистика:
Всего элементов: 20
Средняя длина: 15.75
...
```

---

## 💡 Что можно улучшить/добавить

1. **Конфигурация через файл**
   - Настройка количества потоков
   - Пути к файлам

2. **Обработка разных форматов**
   - JSON
   - XML
   - CSV

3. **База данных**
   - Загрузка из БД
   - Сохранение в БД

4. **Логирование**
   - SLF4J + Logback
   - Запись логов в файл

5. **Веб-интерфейс**
   - Spring Boot REST API
   - Загрузка файлов через веб

6. **Unit-тесты**
   - JUnit 5
   - Mockito

---

## 📖 Как изучать

### Шаг 1: Запусти и посмотри результат
```bash
javac -d bin -sourcepath src/main/java src/main/java/com/example/dataprocessor/*.java
java -cp bin com.example.dataprocessor.Main
cat data/output.txt
```

### Шаг 2: Прочитай EXPLANATION.md
Построчное объяснение всего кода с азов.

### Шаг 3: Изучи VISUAL.md
Посмотри на диаграммы и схемы — так проще понять архитектуру.

### Шаг 4: Экспериментируй (HOWTO.md)
- Измени входные данные
- Создай свой обработчик
- Измени количество потоков

### Шаг 5: Решай задачи (FAQ.md)
В FAQ есть задачи для самопроверки.

---

## 🛠️ Требования

- **Java:** 8 или выше
- **ОС:** Windows, Linux, macOS
- **Редактор:** Любой (IntelliJ IDEA, Eclipse, VS Code, Nano)

---

## 🎓 Изученные технологии

- ✅ Java 8+ Features
- ✅ Annotations
- ✅ Reflection API
- ✅ Stream API
- ✅ Lambda Expressions
- ✅ Method References
- ✅ Concurrency (ExecutorService, Future)
- ✅ File I/O
- ✅ Collections

---

## 📞 Полезные ссылки

- [Java 8 Stream API Docs](https://docs.oracle.com/javase/8/docs/api/java/util/stream/Stream.html)
- [Java Concurrency Tutorial](https://docs.oracle.com/javase/tutorial/essential/concurrency/)
- [Baeldung Java Tutorials](https://www.baeldung.com/)

---

## ✨ Основные преимущества архитектуры

1. **Гибкость** — легко добавить новый обработчик
2. **Масштабируемость** — легко изменить количество потоков
3. **Читаемость** — код понятен благодаря Stream API
4. **Производительность** — многопоточная обработка ускоряет работу
5. **Расширяемость** — можно добавить новые источники данных

---

## 🎉 Результаты выполнения

При запуске программа:
1. Загружает 20 строк из файла
2. Регистрирует 3 обработчика (9 методов)
3. Выполняет обработку параллельно в 4 потоках
4. Генерирует 143 результата
5. Сохраняет всё в output.txt

**Время выполнения:** < 1 секунды
**Используемые потоки:** pool-1-thread-1, pool-1-thread-2, pool-1-thread-3, pool-1-thread-4

---

## 📝 Лицензия

Учебный проект для МТУСИ. Свободное использование в образовательных целях.

---

## 🙏 Благодарности

Проект создан для демонстрации современных возможностей Java 8+ в контексте лабораторной работы.

---

**Успехов в изучении! 🚀**

*Если что-то непонятно — смотри EXPLANATION.md, VISUAL.md, HOWTO.md и FAQ.md*
